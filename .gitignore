# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/

# DB + secrets — NEVER commit
*.db
*.sqlite
*.sqlite3
jobsearch.db
.env
*.env
credentials.json
token.json
*token*.json
client_secret*.json
*.key
*.pem

# Logs (can contain email / job data — keep out)
*.log
logs/

# Frontend
node_modules/
frontend/dist/

# Single-port production build (regenerable)
backend/static/

# OS / editor cruft
.DS_Store
Thumbs.db

# One-off diagnostic scripts — not core to the app, no version history needed
scripts/

# Claude Code local settings — machine-specific permission grants, not shared
# project config (project skills under .claude/skills/ are still tracked)
.claude/settings.json
